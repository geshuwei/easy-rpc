spring:
  application:
    name: easy-rpc-server-example

server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://10.26.15.154:7001/eureka/
  instance:
    hostname: localhost